/**
 * Runtime Configuration
 * This file is loaded by index.html before the main app.
 * It allows changing backend URLs without rebuilding the frontend.
 */
window.E3_CONFIG = {
    // Terrain Server URL
    terrainUrl: 'http://localhost:4444/terrain/',

    // Optional: Base Map Service URL (if applicable)
    baseMapUrl: ''
};
